local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- --- LOGIN DATENBANK ---
local Logins = {
    ["1"] = "1",
    ["Admin"] = "champ123",
    ["User1"] = "Login55",
    ["Tester"] = "Passwort1",
    ["Guest"] = "Guest2025"
}

-- --- ANTI-CHEAT BYPASS ---
local function ApplyBypass()
    local g = getgenv and getgenv() or _G
    if hookmetamethod then
        local oldNamecall
        oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
            local method = getnamecallmethod()
            if not checkcaller() and (method == "Kick" or method == "kick") then
                return nil
            end
            return oldNamecall(self, ...)
        end)
        
        local oldIndex
        oldIndex = hookmetamethod(game, "__index", function(self, key)
            if not checkcaller() and self:IsA("Humanoid") then
                if key == "WalkSpeed" or key == "JumpPower" or key == "JumpHeight" then
                    return key == "WalkSpeed" and 16 or 50
                end
            end
            return oldIndex(self, key)
        end)
    end
end
ApplyBypass()

-- --- HAUPT-FUNKTION (DER HUB) ---
local function StartScript()
    local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
    local Window = Rayfield:CreateWindow({
       Name = "⚜️Sz Premium Hub⚜️ by @champ_326    on dc",
       LoadingTitle = "Massive Boost System",
       LoadingSubtitle = "By 666",
       ConfigurationSaving = {
          Enabled = true,
          FolderName = "Configs",
          FileName = "PlayerModule"
       }
    })

    -- --- INITIALISIERUNG ---
    local Player = game.Players.LocalPlayer
    local Character = Player.Character or Player.CharacterAdded:Wait()
    local Humanoid = Character:WaitForChild("Humanoid")
    local RootPart = Character:WaitForChild("HumanoidRootPart")
    local RunService = game:GetService("RunService")
    local UserInputService = game:GetService("UserInputService")
    local Lighting = game:GetService("Lighting")
    local Mouse = Player:GetMouse()
    local Camera = workspace.CurrentCamera

    local SpawnFolder = workspace:FindFirstChild("SpawnedObjects") or Instance.new("Folder", workspace)
    SpawnFolder.Name = "SpawnedObjects"

    local ColorCorrection = Lighting:FindFirstChild("WorldColor") or Instance.new("ColorCorrectionEffect")
    ColorCorrection.Name = "WorldColor"
    ColorCorrection.Parent = Lighting

    -- --- GLOBALE VARIABLEN ---
    local currentSpeed = 16
    local flySpeed = 50
    local flying = false
    local infJump = false
    local noclip = false
    local freecamActive = false
    local freecamTP = false
    local flingActive = false
    local flingSpeed = 10000
    local sabSpamActive = false
    local stairSteps = 10
    local ladderLength = 10

    -- --- AIMBOT & ESP VARIABLEN ---
    local aimbotEnabled = false
    local espEnabled = false
    local espColor = Color3.fromRGB(255, 0, 0)

    -- --- UPDATER FÜR CHARACTER ---
    Player.CharacterAdded:Connect(function(newChar)
        Character = newChar
        Humanoid = newChar:WaitForChild("Humanoid")
        RootPart = newChar:WaitForChild("HumanoidRootPart")
    end)

    -- --- AIMBOT LOGIK ---
    local function getClosestPlayer()
        local closest = nil
        local shortestDistance = math.huge
        for _, v in pairs(game.Players:GetPlayers()) do
            if v ~= Player and v.Character and v.Character:FindFirstChild("Head") then
                if v.Team ~= Player.Team or v.Team == nil then
                    local pos, onScreen = Camera:WorldToViewportPoint(v.Character.Head.Position)
                    if onScreen then
                        local distance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Mouse.X, Mouse.Y)).Magnitude
                        if distance < shortestDistance then
                            closest = v
                            shortestDistance = distance
                        end
                    end
                end
            end
        end
        return closest
    end

    RunService.RenderStepped:Connect(function()
        if aimbotEnabled then
            local target = getClosestPlayer()
            if target and target.Character and target.Character:FindFirstChild("Head") then
                Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Character.Head.Position)
            end
        end
    end)

    -- --- ESP LOGIK ---
    local function updateESP()
        for _, v in pairs(game.Players:GetPlayers()) do
            if v ~= Player and v.Character and v.Character:FindFirstChild("Head") then
                local espPart = v.Character:FindFirstChild("GeminiESP")
                local nameTag = v.Character:FindFirstChild("GeminiName")
                
                if espEnabled then
                    if not espPart then
                        local highlight = Instance.new("Highlight", v.Character)
                        highlight.Name = "GeminiESP"
                        highlight.FillTransparency = 1
                    end
                    if not nameTag then
                        local bill = Instance.new("BillboardGui", v.Character)
                        bill.Name = "GeminiName"
                        bill.AlwaysOnTop = true
                        bill.Size = UDim2.new(0, 100, 0, 50)
                        bill.Adornee = v.Character.Head
                        bill.StudsOffset = Vector3.new(0, 3, 0)
                        local lab = Instance.new("TextLabel", bill)
                        lab.BackgroundTransparency = 1
                        lab.Size = UDim2.new(1, 0, 1, 0)
                        lab.TextScaled = true
                        lab.Text = v.Name
                    end
                    v.Character.GeminiESP.OutlineColor = espColor
                    v.Character.GeminiName.TextLabel.TextColor3 = espColor
                else
                    if espPart then espPart:Destroy() end
                    if nameTag then nameTag:Destroy() end
                end
            end
        end
    end
    RunService.Heartbeat:Connect(updateESP)

    -- --- PLAYER TAB ---
    local PlayerTab = Window:CreateTab("Player", 4483362458)
    PlayerTab:CreateSection("Movement Controls")

    PlayerTab:CreateSlider({
       Name = "WalkSpeed",
       Range = {16, 500},
       Increment = 1,
       CurrentValue = 16,
       Callback = function(Value)
          currentSpeed = Value
          Humanoid.WalkSpeed = Value
       end,
    })

    task.spawn(function()
        while task.wait(0.1) do
            if Humanoid and Humanoid.Parent then Humanoid.WalkSpeed = currentSpeed end
        end
    end)

    PlayerTab:CreateSlider({
       Name = "JumpPower",
       Range = {50, 500},
       Increment = 1,
       CurrentValue = 50,
       Callback = function(Value)
          Humanoid.JumpPower = Value
       end,
    })

    PlayerTab:CreateSlider({
       Name = "Gravity",
       Range = {0, 196.2},
       Increment = 0.1,
       CurrentValue = 196.2,
       Callback = function(Value)
          workspace.Gravity = Value
       end,
    })

    PlayerTab:CreateSlider({
       Name = "Field of View (FOV)",
       Range = {30, 120},
       Increment = 1,
       CurrentValue = 70,
       Callback = function(Value)
          Camera.FieldOfView = Value
       end,
    })

    PlayerTab:CreateToggle({
       Name = "Infinite Jump",
       CurrentValue = false,
       Callback = function(Value)
          infJump = Value
       end,
    })

    UserInputService.JumpRequest:Connect(function()
        if infJump and Humanoid then
            Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)

    PlayerTab:CreateSection("Fly System")
    PlayerTab:CreateSlider({
       Name = "Fly Speed",
       Range = {10, 500},
       Increment = 1,
       CurrentValue = 50,
       Callback = function(Value)
          flySpeed = Value
       end,
    })

    PlayerTab:CreateToggle({
       Name = " Fly",
       CurrentValue = false,
       Callback = function(Value)
          flying = Value
          if flying then
             local bv = Instance.new("BodyVelocity", RootPart)
             bv.Name = "FlyVel"
             bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
             local bg = Instance.new("BodyGyro", RootPart)
             bg.Name = "FlyGyro"
             bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
             task.spawn(function()
                while flying do
                   local dir = Vector3.new(0,0,0)
                   if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
                   if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
                   if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
                   if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
                   if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0,1,0) end
                   if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then dir = dir - Vector3.new(0,1,0) end
                   bv.Velocity = dir * flySpeed
                   bg.CFrame = Camera.CFrame
                   RunService.RenderStepped:Wait()
                end
                bv:Destroy()
                bg:Destroy()
             end)
          end
       end,
    })

    PlayerTab:CreateSection("Wall Controls")
    PlayerTab:CreateToggle({
       Name = "Noclip",
       CurrentValue = false,
       Callback = function(Value)
          noclip = Value
       end,
    })

    -- --- FLING LOGIK (ERSETZT DURCH FLN-X METHODE) ---
    task.spawn(function()
        RunService.PostSimulation:Connect(function()
            if flingActive and RootPart then
                local velocity = RootPart.Velocity
                RootPart.Velocity = velocity * 10000 + Vector3.new(0, 10000, 0)
                RunService.RenderStepped:Wait()
                RootPart.Velocity = velocity
            end
        end)
    end)

    RunService.Stepped:Connect(function()
        if (noclip or flingActive) and Character then
            for _, part in ipairs(Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end)

    -- --- WORLD TAB ---
    local WorldTab = Window:CreateTab("World", 4483362458)
    WorldTab:CreateSection("Camera Tools")
    WorldTab:CreateToggle({
       Name = "Freecam",
       CurrentValue = false,
       Callback = function(Value)
          freecamActive = Value
          if freecamActive then
              Camera.CameraType = Enum.CameraType.Scriptable
              task.spawn(function()
                  while freecamActive do
                      local camCFrame = Camera.CFrame
                      local moveDir = Vector3.new(0,0,0)
                      if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camCFrame.LookVector end
                      if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camCFrame.LookVector end
                      if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camCFrame.RightVector end
                      if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camCFrame.RightVector end
                      if UserInputService:IsKeyDown(Enum.KeyCode.E) then moveDir = moveDir + Vector3.new(0,1,0) end
                      if UserInputService:IsKeyDown(Enum.KeyCode.Q) then moveDir = moveDir - Vector3.new(0,1,0) end
                      Camera.CFrame = camCFrame + (moveDir * 1.5)
                      RunService.RenderStepped:Wait()
                  end
              end)
          else
              Camera.CameraType = Enum.CameraType.Custom
          end
       end,
    })

    WorldTab:CreateToggle({
       Name = "Teleport to Click (Freecam)",
       CurrentValue = false,
       Callback = function(Value)
          freecamTP = Value
       end,
    })

    Mouse.Button1Down:Connect(function()
        if freecamActive and freecamTP and RootPart then
            Character:MoveTo(Mouse.Hit.p + Vector3.new(0, 3, 0))
        end
    end)

    WorldTab:CreateSection("World Visuals")
    WorldTab:CreateButton({Name = "Day Time", Callback = function() Lighting.ClockTime = 14 end})
    WorldTab:CreateButton({Name = "Night Time", Callback = function() Lighting.ClockTime = 0 end})
    WorldTab:CreateColorPicker({
        Name = "World Color Filter",
        Color = Color3.fromRGB(255, 255, 255),
        Callback = function(Value) ColorCorrection.TintColor = Value end
    })
    WorldTab:CreateButton({
       Name = "Reset World",
       Callback = function()
          ColorCorrection.TintColor = Color3.fromRGB(255, 255, 255)
          Lighting.ClockTime = 14
          workspace.Gravity = 196.2
          Camera.FieldOfView = 70
       end,
    })

    -- --- EXPLOITS TAB ---
    local ExploitsTab = Window:CreateTab("Exploits", 4483362458)
    ExploitsTab:CreateSection("Combat")

    local aimToggle = ExploitsTab:CreateToggle({
       Name = "Aimbot ",
       CurrentValue = false,
       Callback = function(Value) aimbotEnabled = Value end,
    })

    local espToggle = ExploitsTab:CreateToggle({
       Name = "ESP (Box & Name)",
       CurrentValue = false,
       Callback = function(Value) espEnabled = Value end,
    })

    ExploitsTab:CreateColorPicker({
        Name = "ESP Color",
        Color = Color3.fromRGB(255, 0, 0),
        Callback = function(Value) espColor = Value end
    })

    ExploitsTab:CreateKeybind({
       Name = "Toggle Combat Tools",
       CurrentKeybind = "E",
       Callback = function()
          aimbotEnabled = not aimbotEnabled
          espEnabled = not espEnabled
          aimToggle:Set(aimbotEnabled)
          espToggle:Set(espEnabled)
       end,
    })

    ExploitsTab:CreateSection("Troll")
    ExploitsTab:CreateToggle({
       Name = "Easy Sab steal",
       CurrentValue = false,
       Callback = function(Value)
          if Value then
             Humanoid.PlatformStand = true
             RootPart.CFrame = RootPart.CFrame * CFrame.Angles(math.rad(-90), 0, 0)
          else
             Humanoid.PlatformStand = false
             RootPart.CFrame = RootPart.CFrame * CFrame.Angles(math.rad(90), 0, 0)
          end
       end,
    })

    ExploitsTab:CreateToggle({
       Name = "Spam Sab steal (Activate to make it work)",
       CurrentValue = false,
       Callback = function(Value)
          sabSpamActive = Value
          if sabSpamActive then
              task.spawn(function()
                  while sabSpamActive do
                      Humanoid.PlatformStand = true
                      RootPart.CFrame = RootPart.CFrame * CFrame.Angles(math.rad(-90), 0, 0)
                      task.wait(0.05)
                      Humanoid.PlatformStand = false
                      RootPart.CFrame = RootPart.CFrame * CFrame.Angles(math.rad(90), 0, 0)
                      task.wait(0.05)
                  end
              end)
          end
       end,
    })

    ExploitsTab:CreateSlider({
       Name = "Fling Power",
       Range = {5000, 100000},
       Increment = 5000,
       CurrentValue = 20000,
       Callback = function(Value) flingSpeed = Value end,
    })

    ExploitsTab:CreateToggle({
       Name = "Ghost Fling (No-Spin Char)",
       CurrentValue = false,
       Callback = function(Value) 
           flingActive = Value 
       end,
    })

    -- PLAYER TP FUNKTION
    local function getList()
        local list = {}
        for _, v in pairs(game.Players:GetPlayers()) do if v ~= Player then table.insert(list, v.Name) end end
        return list
    end
    local TPDrop = ExploitsTab:CreateDropdown({
        Name = "Player Teleport",
        Options = getList(),
        CurrentOption = "",
        Callback = function(Option)
            local target = game.Players:FindFirstChild(Option[1])
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                RootPart.CFrame = target.Character.HumanoidRootPart.CFrame
            end
        end,
    })
    task.spawn(function() while task.wait(5) do TPDrop:Refresh(getList()) end end)

    -- --- OBJECTS TAB ---
    local ObjectsTab = Window:CreateTab("Objects", 4483362458)
    ObjectsTab:CreateSection("Plank Tools")
    ObjectsTab:CreateButton({
       Name = "Spawn Plank",
       Callback = function()
          if RootPart then
             local Plank = Instance.new("Part", SpawnFolder)
             Plank.Size = Vector3.new(25, 1, 5)
             Plank.CFrame = RootPart.CFrame * CFrame.new(0, -1, -15)
             Plank.Anchored = true
             Plank.Material = Enum.Material.Wood
          end
       end,
    })

    ObjectsTab:CreateSection("Stair Tools")
    ObjectsTab:CreateInput({
       Name = "Anzahl Stufen",
       PlaceholderText = "Zahl...",
       Callback = function(Text) stairSteps = tonumber(Text) or 10 end,
    })
    ObjectsTab:CreateButton({
       Name = "Spawn Treppe",
       Callback = function()
          if RootPart then
             local startPos = RootPart.CFrame * CFrame.new(0, -1, -5)
             for i = 1, stairSteps do
                local Step = Instance.new("Part", SpawnFolder)
                Step.Size = Vector3.new(6, 1, 2)
                Step.CFrame = startPos * CFrame.new(0, i, -i * 2)
                Step.Anchored = true
                Step.Material = Enum.Material.Concrete
             end
          end
       end,
    })

    ObjectsTab:CreateSection("Ladder Tools")
    ObjectsTab:CreateInput({
       Name = "Leiter Länge",
       PlaceholderText = "Zahl...",
       Callback = function(Text) ladderLength = tonumber(Text) or 10 end,
    })
    ObjectsTab:CreateButton({
       Name = "Spawn Leiter",
       Callback = function()
          if RootPart then
             local startPos = RootPart.CFrame * CFrame.new(0, 0, -4)
             for i = 1, ladderLength do
                local Rung = Instance.new("Part", SpawnFolder)
                Rung.Size = Vector3.new(4, 0.3, 0.3)
                Rung.CFrame = startPos * CFrame.new(0, i * 1.5, 0)
                Rung.Anchored = true
                local Truss = Instance.new("TrussPart", Rung)
                Truss.Size = Vector3.new(2, 2, 2)
                Truss.CFrame = Rung.CFrame
                Truss.Transparency = 1
                Truss.Anchored = true
             end
          end
       end,
    })

    ObjectsTab:CreateSection("Cleanup")
    ObjectsTab:CreateButton({
       Name = "Delete All Objects",
       Callback = function() SpawnFolder:ClearAllChildren() end,
    })

    -- --- UPDATES TAB ---
    local UpdatesTab = Window:CreateTab("Updates", 4483362458)
    UpdatesTab:CreateLabel(" if you want anything to be added to this script dm me on Discord Snowy_champ")

    Rayfield:Notify({Title = "Sz Premium", Content = "Hub loaded .", Duration = 5})
end

-- --- LOGIN SYSTEM ---
local LoginWindow = Rayfield:CreateWindow({
   Name = "Sz Login System",
   LoadingTitle = "Auth...",
})

local LoginTab = LoginWindow:CreateTab("Auth", 4483362458)
local TempUser = ""
local TempPass = ""

LoginTab:CreateInput({
   Name = "Username",
   PlaceholderText = "Username...",
   Callback = function(Value) TempUser = Value end,
})

LoginTab:CreateInput({
   Name = "Password",
   PlaceholderText = "Passwort...",
   Callback = function(Value) TempPass = Value end,
})

LoginTab:CreateButton({
   Name = "Login",
   Callback = function()
      if Logins[TempUser] and Logins[TempUser] == TempPass then
          Rayfield:Notify({Title = "Erfolgreich", Content = "Lade Hub...", Duration = 3})
          task.wait(1)
          Rayfield:Destroy() 
          task.wait(0.5)
          StartScript()
      else
          Rayfield:Notify({Title = "Fehler", Content = "Login oder Passwort falsch.", Duration = 3})
      end
   end,
})
